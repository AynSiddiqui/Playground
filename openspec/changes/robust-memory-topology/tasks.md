## 1. Registry & Pointer Fixes
- [x] 1.1 In `MemoryCanvas.tsx::buildNodesAndEdges`, inside the `(snapshot.stack || []).forEach` loop, define `const isPointer = local.type && local.type.includes('*');`.
- [x] 1.2 Update the `addressToNodeId.set(local.address, nodeId);` call to only execute if `!isPointer`.
- [x] 1.3 Update the `targetAddr` extraction: `const targetAddr = isPointer ? (local.address || local.value) : null;`. If it is NOT a pointer, it should never emit an `edge-stack-var` pointer edge.

## 2. Structural Deduplication
- [x] 2.1 At the top of `buildNodesAndEdges`, declare `const processedStructuralLinks = new Set<string>();`.
- [x] 2.2 Inside both of the `Object.entries(links.nodes).forEach` loops (for stack and heap), add a check right after `const resolvedId`: `if (processedStructuralLinks.has(resolvedId)) return; processedStructuralLinks.add(resolvedId);`.

## 3. Unify UI Rendering
- [x] 3.1 In `MemoryNode.tsx`, locate the `advancedData.structure === 'tree_node'` and `advancedData.structure === 'list_node'` blocks at the bottom of the component.
- [x] 3.2 Completely remove all `<Handle>` elements from within these blocks so they do not clash with the Handles dynamically generated by the main `variables.map` block.
