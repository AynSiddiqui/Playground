## 1. Adjacency and Delta Propagation

- [x] 1.1 Build a structural `childrenMap` from edges in `getLayoutedElements` inside `frontend/src/utils/graphBuilder.ts`
- [x] 1.2 Implement DFS `propagate` function to compute and distribute manual layout deltas down the graph structure
- [x] 1.3 Apply the resolved deltas to set the final layout coordinates of all nodes in `getLayoutedElements`

## 2. Spawning Code Cleanup

- [x] 2.1 Remove the legacy `isNew` parent-relative coordinates block from `getLayoutedElements` in `frontend/src/utils/graphBuilder.ts`
